# redis-cli

Start the server:

    redis-server
    
Command line client:

    redis-cli
    redis> set some_key "some awesome val"
    redis> get some_key
    redis> incr counter
    redis> incr counter
    redis> incr counter
    redis> get counter
